name: ��� Trigger bot HTTP endpoint

on:
  schedule:
    - cron: '31 1 * * *'   # เวลาไทย 08:31 (UTC+7)
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger bot endpoint
        run: |
          curl -X GET https://exchange-rate-bot--0000010.bravegrass-436d065b.westus2.azurecontainerapps.io/run
